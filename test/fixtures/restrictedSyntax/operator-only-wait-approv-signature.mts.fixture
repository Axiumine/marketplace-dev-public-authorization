// The gate declared on the login-check interface — where the widened projection has to land for the
// read to typecheck. `IShopOwnerLoginCheckData` in marketplace-dev-public-authorization carries it.
export interface IShopOwnerLoginCheckData {
	waitApprov?: boolean
}
